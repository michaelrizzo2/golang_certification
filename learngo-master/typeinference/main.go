package main

import (
	"fmt"
)

func main() {
	safe := true
	fmt.Println(safe)

	//var name type=value is one way
	//another way is to have go guess the type from the output var name=value
	//the final way is name:=value
}
